{
  "name": "screenless-gemini-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:key": "wrangler secret put GEMINI_API_KEY",
    "secret:token": "wrangler secret put APP_TOKEN"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
